<div id="list_app"
    data-list="<%= @list.to_json %>"
    data-mountains="<%= @mountains.to_json %>"
    data-mountain="<%= @mountain.to_json %>">
</div>

<% content_for :after_js do %>
  <%= javascript_pack_tag "list" %>
<% end %>